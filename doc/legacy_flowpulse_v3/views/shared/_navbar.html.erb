<!-- app/views/shared/_navbar.html.erb -->
<nav class="bg-white border-b border-slate-200 shadow-sm">
  <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between h-16 items-center">
      <div class="flex items-center space-x-3">
        <%= link_to root_path, class: "font-bold text-lg text-slate-800 hover:text-slate-900" do %>
          <%= (Current.domain&.title || "PosturaCorretta") %>
          <% if (root = Current.taxbranch) %>
            <% root.children.home_nav.each do |b| %>
              <%= link_to b.display_label, gate_path(b.slug) %>
            <% end %>
          <% end %>
        <% end %>
      </div>
      <div class="hidden sm:flex space-x-6">
        <% if (root = Current.domain&.taxbranch) %>
          <% root.children.home_nav.each do |b| %>
            <%= link_to b.display_label, gate_path(b.slug), class: "text-slate-700 hover:text-slate-900 font-medium" %>
          <% end %>
        <% else %>
          <%= link_to "Home", root_path, class: "text-slate-700 hover:text-slate-900 font-medium" %>
        <% end %>
      </div>
    </div>
  </div>
</nav>


