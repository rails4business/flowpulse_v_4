json.extract! slot_template, :id, :lead_id, :title, :description, :day_of_week, :time_start, :time_end, :repeat_rule, :repeat_every, :repeat_start, :repeat_end, :seasons, :jsonb, :color_hex, :taxbranch_id, :created_at, :updated_at
json.url slot_template_url(slot_template, format: :json)
