json.extract! taxbranch, :id, :lead_id, :notes, :slug, :slug_category, :slug_label, :ancestry, :position, :meta, :execution_mode, :performed_by_roles, :target_roles, :created_at, :updated_at
json.url taxbranch_url(taxbranch, format: :json)
