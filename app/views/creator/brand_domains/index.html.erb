<% content_for :title, "Domini Brand" %>

<section class="dashboard-shell">
  <div class="app-layout">
    <%= render "shared/app_sidebar" %>

    <div class="dashboard-card">
      <div style="display: flex; justify-content: space-between; align-items: center; gap: 1rem; flex-wrap: wrap;">
        <div>
          <p class="eyebrow">Creator Brand Domain</p>
          <h1 style="margin-bottom: 0.35rem; max-width: none; white-space: nowrap;">Domini di <%= @brand_port.name %></h1>
        </div>
        <div style="display: flex; gap: 0.75rem;">
          <%= link_to "Torna al brand", creator_port_path(@brand_port), class: "btn btn-secondary" %>
          <%= link_to "+ Dominio", new_creator_port_brand_domain_path(@brand_port), class: "btn btn-primary" %>
        </div>
      </div>

      <div style="margin-top: 1.5rem; display: grid; gap: 1rem;">
        <% @brand_domains.each do |brand_domain| %>
          <div class="dashboard-actions" style="background: #fff; padding: 1rem; border-radius: 16px; border: 1px solid #e2e8f0;">
            <div style="display: flex; justify-content: space-between; gap: 1rem; flex-wrap: wrap;">
              <div>
                <strong><%= brand_domain.host %></strong>
                <p style="margin: 0.35rem 0 0; color: #475569;">Lingua: <%= brand_domain.locale %> · Home: <%= brand_domain.home_page_key.present? ? brand_domain.home_page_key : "brand" %></p>
                <p style="margin: 0.35rem 0 0; font-size: 0.82rem; color: <%= brand_domain.published? ? "#166534" : "#991b1b" %>;">
                  <strong><%= brand_domain.published? ? "Pubblico attivo" : "Non pubblicato" %></strong>
                  · <%= brand_domain.published? ? "Il resolver pubblico usa questo dominio." : "Finché non è pubblicato, la root pubblica lo ignora." %>
                </p>
              </div>
              <div style="display: flex; gap: 0.5rem; align-items: center;">
                <% if brand_domain.primary? %><span class="btn btn-secondary" style="padding: 0.35rem 0.6rem;">Primary</span><% end %>
                <%= button_to(
                  brand_domain.published? ? "Published" : "Draft",
                  toggle_published_creator_port_brand_domain_path(@brand_port, brand_domain),
                  method: :patch,
                  class: "btn btn-secondary",
                  form: {
                    style: "display: inline-block;",
                    data: (brand_domain.published? ? { turbo_confirm: "Sei sicuro di voler riportare questo dominio in draft?" } : {})
                  },
                  style: "padding: 0.35rem 0.6rem; #{brand_domain.published? ? "background: #dcfce7; border-color: #86efac; color: #166534;" : "background: #fee2e2; border-color: #fecaca; color: #991b1b;"}"
                ) %>
                <%= link_to "Modifica", edit_creator_port_brand_domain_path(@brand_port, brand_domain), class: "btn btn-secondary" %>
              </div>
            </div>
          </div>
        <% end %>

        <% if @brand_domains.blank? %>
          <div class="dashboard-actions" style="background: #fff; padding: 1rem; border-radius: 16px; border: 1px dashed #cbd5e1;">
            Nessun dominio associato a questo brand.
          </div>
        <% end %>
      </div>
    </div>
  </div>
</section>
